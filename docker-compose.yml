services:
  scoreboard:
    build: .
    ports:
      - "8080:3001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin1234}
    restart: unless-stopped
